#include "main.h"
#include <string.h>

/**
 * char *_strcpy - copy string
 * @dest: copy destination
 * @src: copyb string
 * Return: Results
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
